{
  "name": "mock-rest-api",
  "version": "1.0.0",
  "description": "Task 6: Postman. Postman + Newman automation",
  "scripts": {
    "tern-on-api": "cp mockApi/db_back_up.yaml mockApi/db_stage.yaml && npx yaml-server --hotReload=off --autoStart=off --port 3000 --database ./mockApi/db_stage.yaml",
    "tests:newman": "newman run store.collection.json"
  },
  "keywords": ["api","mock","yaml-server","rest"],
  "author": "Anna Ruban",
  "license": "ISC",
  "dependencies": {
    "yaml-server": "^1.10.0"
  }
}
